import '@/styles/components/loader.css';

const Loader = () => {
  return (
    <div className='loader__container' aria-label='Loading signs'>
      <span className='loader' />
    </div>
  );
};

export default Loader;
